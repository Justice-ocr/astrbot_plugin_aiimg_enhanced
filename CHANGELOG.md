# 更新日志

## [未发布]

## [v4.9.2] - 2026-09-20

### NAI Images 协议适配

- OpenAI Images 模板补充可选自然语言转 NAI 标签，支持指定转换 LLM 或跟随当前会话，默认关闭。
- 文生图和改图均复用原有 Images 协议；转换失败不提交该服务商的图片请求，保留原有参考图传递，不注入 NAI 原生参数。

## [v4.9.1] - 2026-09-20

### NAI Chat 协议适配

- OpenAI Chat图模板新增可选 NAI 自然语言转标签，适用于通过 `/v1/chat/completions` 提供 NAI 模型的中转站；文生图及改图保留原有 Chat 请求协议，默认关闭转换。
- 支持指定转换 LLM 或跟随当前会话；转换失败不提交该服务商的图片请求，不注入未经确认的 NAI 原生参数。

## [v4.9.0] - 2026-09-20

### NovelAI 中转服务商

- 新增 NovelAI 原生协议中转模板，接入重绘、单图角色保持、多图 Vibe、ZIP 图片响应和有界内存编码缓存，复用现有改图与自拍入口。
- 两种 NovelAI 模板支持可选自然语言转标签，可指定 LLM 或跟随当前会话；转换失败时停止该服务商的图片提交。
- 新增 NovelAI 第三方 GET 网关模板，按 `nai_brush` 的 tag/model 协议接入文生图；支持自定义路径、Token/Bearer 鉴权、画师标签、负面词、采样参数及图片 URL/Base64/二进制响应。
- 原生模板按服务商配置选择重绘、角色保持或 Vibe，需中转站支持对应接口；暂不支持逐图职责自动映射、视觉参数提取、多角色布局或历史参数快照。

## [v4.8.1] - 2026-09-17

### OpenAI Videos Base64 提交兼容修复

- 兼容部分网关无法通过 multipart 文本字段解析大体积 Data URI 的情况；出现 `NextPart: bufio: buffer full` 时自动改用 JSON 重新提交。
- JSON 回退支持 `image_urls`、`image_with_roles` 和首尾帧字段，真实文件形式的 `input_reference` 仍保持 multipart 上传。

## [v4.8.0] - 2026-09-17

### OpenAI Videos 多参考图输入

- OpenAI Videos 模板新增最多 9 张 `image_urls` 参考图，支持公网 URL 与 JPEG、PNG、WebP 的 Data URI Base64。
- 新增首尾帧模式，可通过 `first_frame_image` 与 `last_frame_image` 提交严格配对的两张图片。
- 新增 `image_with_roles` 模式，支持 `reference_image`、`first_frame` 和 `last_frame` 角色标记。
- 新增自动、单图上传、多参考图、首尾帧、角色参考图和角色首尾帧六种输入模式，并保留原有 `input_reference` 兼容行为。
- 新增视频分辨率与随机种子配置，并按 MiniMax H3 的输入模式校验时长和分辨率范围。
- 普通参考图模式可在消息无图片时自动使用当前会话的人设/自拍参考图；首尾帧模式不会混入自动参考图。

## [v4.7.2] - 2026-09-17

### 本地视频跨容器发送修复

- OpenAI Videos 下载到 AstrBot 本地的视频在平台侧路径不可见时，自动改用 `Video.fromBase64` 发送。
- 为 Base64 回退增加文件大小限制，避免超大视频占用过多内存；可通过 `features.video.base64_fallback_max_mb` 调整。
- 本地视频发送失败后不再将文件路径误当作 URL，也不再把服务器本地路径作为文本发送给用户。
- 两种视频发送方式均失败时任务会正确进入失败流程，不再错误记录为生成完成。

## [v4.7.1] - 2026-09-17

### OpenAI Videos 尺寸格式修复

- 自动将 `16：9` 等全角冒号画幅转换为接口要求的 `16:9`，并移除尺寸中的多余空格。
- 自动将 `1280×720` 和大写 `X` 像素尺寸规范化为 `1280x720`，避免兼容网关返回 `Invalid size`。

## [v4.7.0] - 2026-09-17

### OpenAI Videos 服务商

- 新增通用的 `OpenAI Videos` 服务商模板，支持官方及兼容网关的 `/v1/videos` 协议。
- 使用 multipart 表单提交文生视频或单张参考图任务，支持自定义模型、时长、尺寸、参考图字段和额外表单参数。
- 支持异步任务状态轮询；任务完成后携带 API Key 从 `/v1/videos/{id}/content` 下载视频并通过本地文件发送。
- Base URL 同时兼容根地址和以 `/v1` 结尾的地址，可配置代理、重试、轮询超时和最大下载大小。
- 修正 LLM 工具中的视频服务商分类，避免 Agnes、MiniMax H3 和 OpenAI Videos 被误列为图片服务商。

## [v4.6.0] - 2026-09-17

### MiniMax H3 视频服务商

- 新增独立的 `MiniMax H3 视频` 服务商模板，接入官方 V2 视频生成与任务查询接口。
- 支持文生视频、单图首帧生成以及最多 9 张图片的多参考图模式；单图请求自动使用 `adaptive` 画幅。
- 本地参考图默认使用 Data URI Base64，也可通过 AstrBot 文件服务公网地址提供参考图 URL；消息中的公网图片地址会直接复用。
- 提供 `768P` / `2K`、4–15 秒、画幅、轮询、重试、超时和代理等 Web 配置项。
- 支持在消息无图片时自动使用当前会话的人设/自拍参考图，并补充服务商配置、请求构造、轮询与取消传播测试。

## [v4.5.2] - 2026-09-16

### Agnes AstrBot 文件服务

- 新增 AstrBot 文件服务参考图传输模式，支持使用面板子域名生成 Agnes 可访问的本地图片 URL。
- 本地参考图会写入插件临时目录并注册文件 token，视频任务结束后自动清理。
- 支持 token 在有效期内重复访问，避免 Agnes 服务端多次读取参考图时链接失效。
- `auto` 模式在配置 AstrBot 文件服务公网地址时优先使用该方式；原有第三方图床和免费公网图床模式保持不变。

## [v4.5.1] - 2026-09-16

### Agnes 自拍参考图回退

- Agnes 视频生成在消息和引用消息均无图片时，自动使用当前会话选择的人设/自拍参考图，最多提交 5 张。
- 公网参考图直接复用 URL，本地参考图按已有上传策略处理，并根据身份、服装、姿势和场景职责补充视频提示词。
- 显式发送的图片仍具有最高优先级；该回退仅对 Agnes 生效，不改变其他视频服务商行为。

## [v4.5.0] - 2026-09-16

### Agnes 视频服务商

- 新增独立的 `Agnes 视频` 服务商模板，接入 `agnes-video-2.5-flash` 异步视频生成与结果轮询。
- 支持文生视频、单图关键帧以及最多 5 张图片的多参考图模式，并优先复用消息中的公网图片地址。
- 本地参考图支持自动上传、第三方图床、免费公共图床或禁用上传，并提供视频时长、画幅、轮询、代理等 Web 配置项。
- 多参考图参数仅传递给声明支持该能力的视频后端，不改变现有视频服务商的调用方式。
- 补充 Agnes 请求、轮询、取消传播、服务商配置校验和多参考图集成测试。

## [v4.4.0] - 2026-09-10

### 图片历史与连续改图（2026-09-10）

- 新增独立 Web「生成历史」页面，支持搜索提示词、分页、查看详情、复制提示词和下载原图，并显示生成会话与时间。
- 全局最多保留 100 张历史图片，按生成时间倒序展示，页面序号从 1 开始；新增图片后序号随之变化，聊天操作使用不重复利用的固定记录 ID。
- 历史元数据与图片副本持久化保存，超限自动清理最旧记录和副本；普通缓存清理不影响历史，发送失败的生成结果也会归档。
- 新增 `/图片历史`、`/重发图片 #记录ID`、`/继续改图 [#记录ID] 修改要求`，支持重发已有结果而不重新生成，以及基于历史图片继续编辑。
- 聊天历史按机器人、消息来源、用户和 AstrBot 会话隔离；已认证的 Web 管理页面可查看全局历史。

### 会话人设与参考图角色（2026-09-10）

- 人设支持按会话独立选择并持久化保存；同一群聊会话共享选择，不影响其他会话。`/切换人设 默认` 可恢复跟随全局默认，被删除的人设自动回退。
- Web「人设管理」新增会话人设选择，调整后立即保存；全局默认与会话选择分别管理。
- 每次生成固定人设快照，中途修改仅影响后续任务，避免并发会话混用人设。
- 每张人设参考图可指定身份、服装、姿势或场景；旧参考图默认作为身份图，自拍至少需要一张可读取的身份图。
- 参考图职责按实际提交的图片编号组织提示词；角色分工效果及参考图数量仍取决于服务商和模型能力。

### 任务管理（2026-09-10）

- 新增 Web「任务管理」页面及 `/任务列表`、`/取消任务 任务ID`，展示图片、批量和后台视频任务的状态、会话、时间、人设及生成/发送数量。
- 支持准备、生成、下载、发送、完成、部分完成、失败和取消状态，任务页面每 3 秒自动刷新。
- 取消任务会停止本地等待及后续处理，并释放并发名额；不能保证上游停止生成或退款，也不会删除已有历史或撤回已发送消息。
- 任务列表仅保存在内存中，最多保留 100 条记录，执行中的任务不淘汰；重载后不恢复，不新增自动排队或付费生成自动重试。

### 验证与界面修复（2026-09-10）

- 补充历史归档、会话隔离、参考图角色、任务取消和生命周期等测试，全量 144 项测试通过。
- 修复参考图角色下拉框被缩略图容器裁剪的问题，并检查桌面和移动端布局。

### Grok Images 修复

- 新增 `Grok Images 改图` 服务商模板；`Grok Chat图` / `OpenAI Chat图` 中配置 `grok-imagine-image-edit` 的旧服务商会自动迁移到该模板后端。
- 对照 nova-image-studio 修正 `grok-imagine-image-edit` 的 JSON 图片字段为 data URL 字符串，并优先请求 URL 格式结果；该模型会直接走 `/v1/images/edits`，CCODE 返回“模型不支持文本对话”的其他图片模型也会切换到该端点，且全程保留原模型不降级。
- 对齐 xAI 官方 Images API：改图改用 `application/json`，单图使用 `image`，多图使用 `images`，不再发送 multipart `image[]`。
- 支持最多三张独立参考图，并将像素输出配置转换为 xAI 的 `aspect_ratio` 与 `1k/2k`。
- 默认模型更新为 `grok-imagine-image-quality`，旧的 `grok-imagine-1.0` / `grok-imagine-1.0-edit` 配置会自动迁移。
- Grok Images 的重试次数恢复为统一语义：`0` 表示只请求一次，`2` 表示失败后最多额外重试两次。
- xAI 返回 `upstream_unavailable` 时会继续按上游故障重试，并保留原模型不做自动降级。

### 文档与插件信息

- 重写 README，增加品牌横幅、快速安装、首次配置、人设参考图、指令说明和常见问题。
- 更新插件作者与仓库地址，统一指向 `Justice-ocr/astrbot_plugin_aiimg_enhanced`。
- 更换插件市场和配置页面使用的品牌图标，不再使用人物照片。
- 移除 README 的第三方徽章图片请求，并按真实文件头校验上传的参考图格式。

## [v4.3.0] - 2026-04-26

### 新增

- 新增文生图预设能力，支持通过 `features.draw.presets` 配置预设，并使用 `/文生图 预设名 补充提示词` 调用。
- 新增统一批量命令入口，支持：
  - `/批量n aiimg ...`
  - `/批量n 文生图 ...`
  - `/批量n aiedit ...`
  - `/批量n 自拍 ...`
  - `/批量n 改图预设名 ...`
- 新增 `LLM` 批量工具 `aiimg_batch_generate`，支持先规划多条不重复提示词，再一次性批量生成整组图片。
- 批量结果统一改为单张直接发送。
- 批量发送不再额外附带“标题 / 提示词 / 状态 / 失败提示”这类通知文本，只保留原插件自己的表情反馈。

### 配置增强

- 新增 `features.draw.batch_concurrency`，单独控制文生图批量并发。
- 新增 `features.edit.batch_concurrency`，单独控制改图 / 自拍批量并发。
- 新增 `features.batch.max_count`，控制单次批量最大数量，默认 `8`。
- 新增 provider 级 `generate_request_mode` / `edit_request_mode`，支持 `auto`、`stream`、`non_stream`。

### LLM 工具与批量规划

- `aiimg_generate` 继续支持 `auto` / `text` / `edit` / `selfie_ref` 路由。
- `aiimg_batch_generate` 默认批量数量为 `4`，建议范围 `2-8`，最终会被 `features.batch.max_count` 限制。
- 批量规划器会要求 `LLM` 输出 `title`、`prompt`、`variation_focus`，并对数量与去重结果做校验。
- 批量规划更适合同场景、同穿搭、不同姿势 / 角度 / 表情的成组出图需求。

### 兼容性与稳定性

- 修复 `ProviderRegistry` 中新默认 `auto` 覆盖旧 `enable_stream_*` 布尔配置的问题。
- 对齐 `ProviderRegistry` 与 `OpenAIChatImageBackend` 的 `request_mode` 兼容语义：
  - 显式 `stream` / `non_stream` 优先
  - `auto + enable_stream_*` 保留旧配置行为
  - 都没有时才回到默认 `auto`
- 统一 `validate()` warning 文案与运行时实际回退行为，避免“文档一套、执行一套”。
- 对单路径 provider 增加 `request_mode` 忽略提示，减少误解。

### 文档与元数据

- 重写 `README.md`，补充新功能的实际调用方式、批量命令、`LLM tool`、并发配置、平台限制与请求模式说明。
- 为插件补充 `metadata.yaml` 中的 `astrbot_version` 与 `support_platforms` 提示。
- 新增本 `CHANGELOG.md`，开始记录版本更新内容。

## [v4.2.26] - 历史基线版本

- 这是补充 `CHANGELOG` 之前的基线版本号。
- 更早的历史更新尚未回填到本文件。
